# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9
python-dotenv==1.0.1

# Knowledge graph
networkx==3.3
neo4j==5.24.0          # optional — app falls back to in-memory graph if unavailable/unconfigured

# Vector retrieval / NLP
scikit-learn==1.5.2    # TF-IDF fallback vector store (always available, no downloads needed)
numpy==1.26.4
#chromadb==0.5.5         # optional — used automatically if installed, else TF-IDF fallback is used
#sentence-transformers==3.1.1   # optional — used for embeddings/reranking if installed
rank_bm25==0.2.2        # optional — a custom numpy BM25 is included and used by default

# Documents
pypdf==5.0.1
python-docx==1.1.2

# Reports
reportlab==4.2.2

# Testing
pytest==8.3.3
httpx==0.27.2
